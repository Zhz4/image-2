# Project Notes

This project is split into:

- `frontend/`: Vue 3 + Vite + TypeScript + Element Plus
- `backend/`: Fastify + TypeScript API service

Use `pnpm dev`, `pnpm lint`, and `pnpm build` from the repository root for the separated app.
